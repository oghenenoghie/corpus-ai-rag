# Nile Postgres connection string (console.thenile.dev -> nile_bronze_brush -> Connection info)
DATABASE_URL=postgres://user:password@host:5432/nile_bronze_brush

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: only needed if using Cohere's hosted rerank instead of the local
# bge-reranker-base cross-encoder (sentence-transformers)
COHERE_API_KEY=
